{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/ai-tool-agent-tutorial/eval_scenarios.schema.json",
  "title": "Tool Agent Eval Scenarios",
  "description": "Deterministic behavioral scenarios for the tool agent eval runner.",
  "type": "array",
  "items": {
    "$ref": "#/$defs/scenario"
  },
  "$defs": {
    "scenario": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "fixtures",
        "turns"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable scenario identifier used in reports."
        },
        "description": {
          "type": "string",
          "description": "Human-readable explanation of the behavior under test."
        },
        "fixtures": {
          "$ref": "#/$defs/fixtures"
        },
        "turns": {
          "type": "array",
          "description": "Conversation turns executed sequentially against one agent instance.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/turn"
          }
        }
      }
    },
    "fixtures": {
      "type": "object",
      "additionalProperties": true,
      "description": "Deterministic tool return values used by the scripted test registry.",
      "properties": {
        "current_time": {
          "type": "string",
          "description": "ISO datetime returned by get_current_time.",
          "examples": [
            "2026-05-08T14:01:32"
          ]
        },
        "directory_listing": {
          "type": "string",
          "description": "ls -al style text returned by list_current_directory."
        }
      }
    },
    "turn": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "input"
      ],
      "properties": {
        "input": {
          "type": "string",
          "description": "User message sent to the agent."
        },
        "llm_responses": {
          "type": "array",
          "description": "Scripted assistant messages returned by the fake LLM for this turn.",
          "items": {
            "$ref": "#/$defs/llm_response"
          }
        },
        "expected_tools": {
          "type": "array",
          "description": "Exact ordered list of tool names expected for this turn.",
          "items": {
            "type": "string"
          }
        },
        "forbidden_tools": {
          "type": "array",
          "description": "Tool names that must not be used on this turn.",
          "items": {
            "type": "string"
          }
        },
        "assertions": {
          "$ref": "#/$defs/assertions"
        }
      }
    },
    "llm_response": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "content": {
          "type": "string",
          "description": "Assistant text returned by the scripted LLM."
        },
        "reasoning_content": {
          "type": "string",
          "description": "Optional reasoning text captured in traces for debugging."
        },
        "tool_calls": {
          "type": "array",
          "description": "Native-looking tool calls returned by the scripted LLM.",
          "items": {
            "$ref": "#/$defs/tool_call"
          }
        }
      }
    },
    "tool_call": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Tool call id. If omitted, the fixture layer creates one."
        },
        "name": {
          "type": "string",
          "description": "Registered tool name to call."
        },
        "arguments": {
          "type": "object",
          "description": "JSON arguments sent to the tool."
        }
      }
    },
    "assertions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "answer_contains": {
          "type": "array",
          "description": "Case-insensitive text fragments that must appear in the final answer.",
          "items": {
            "type": "string"
          }
        },
        "answer_not_contains": {
          "type": "array",
          "description": "Case-insensitive text fragments that must not appear in the final answer.",
          "items": {
            "type": "string"
          }
        },
        "memory_valid": {
          "type": "boolean",
          "description": "Validate assistant tool calls are followed by matching tool messages."
        },
        "grounded_in_tool_results": {
          "type": "boolean",
          "description": "Validate obvious file names, byte sizes, and access claims against tool results."
        },
        "date_answer_matches_tool_result": {
          "type": "boolean",
          "description": "Validate relative date answers against get_current_time output."
        },
        "fallback_used": {
          "type": "boolean",
          "description": "Assert whether a deterministic fallback was used for the final answer."
        }
      }
    }
  }
}
